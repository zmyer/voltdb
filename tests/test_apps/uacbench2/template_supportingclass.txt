package uac;

import org.voltdb.SQLStmt;
import org.voltdb.VoltProcedure;

public class Extra$num {

$stmts

$static_data

    public long method1() {
        long retval = 0;
$random_churn1
        return retval;
    }

    public Object getValue(VoltProcedure proc) {
        long retval = $value;
$random_churn2
        if (retval % 2 == 0) {
            proc.voltQueueSQL(stmt$idxa);
        }
        else {
            proc.voltQueueSQL(stmt$idxb);
        }
        return proc.voltExecuteSQL();
    }
}
