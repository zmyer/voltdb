package uac;

import org.voltdb.SQLStmt;
import org.voltdb.VoltProcedure;

public class Proc$procnum extends VoltProcedure {

$stmts

$static_data

    public long run(long partitionCol) {
        long value = 0;

$call_extras

        return value;
    }
}
